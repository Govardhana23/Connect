{
    "name": "jaegerist-root",
    "private": true,
    "workspaces": [
        "client",
        "server"
    ],
    "scripts": {
        "build": "npm install && npm run build -w client && npm run build -w server",
        "dev": "npm run dev -w client",
        "postinstall": "cd server && npx prisma@5.12.1 generate"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "react-scripts": "5.0.1"
    },
    "dependencies": {
        "firebase": "^12.9.0"
    },
    "engines": {
        "node": "18.x"
    }
}